#pragma once

#include <windows.h>
#include <windowsx.h>
#include <string>

class app_tarcza
{
private:

    static constexpr int CHILD_COUNT = 10;
    static constexpr int CLIENT_SIZE = 512;
    static constexpr int CHILD_SIZE = 36;
    static constexpr int RADIUS = 224;

    static const std::wstring MAIN_CLASS;
    static const std::wstring CHILD_CLASS;

    HINSTANCE m_instance{};
    HWND m_main{};

    bool m_dragging = false;
    int m_drag_id = -1;

    double m_delta = 0;

    static LRESULT CALLBACK main_proc_static(
        HWND, UINT, WPARAM, LPARAM);

    static LRESULT CALLBACK child_proc_static(
        HWND, UINT, WPARAM, LPARAM);

    LRESULT main_proc(HWND, UINT, WPARAM, LPARAM);
    LRESULT child_proc(HWND, UINT, WPARAM, LPARAM);

    bool register_main();
    bool register_child();

    HWND create_main();
    void create_children();

    void layout_children();

    static double initial_angle(int id);

public:

    explicit app_tarcza(HINSTANCE instance);

    int run(int show);
};